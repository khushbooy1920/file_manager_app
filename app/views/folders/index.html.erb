<h1>Folders</h1>

<div id="folders">
  <% @folders.each do |folder| %>
    <div>
      <%= link_to folder.name, files_list_folder_path(folder, format: :js), remote: true, data: { turbo: false } %>
    </div>
  <% end %>
</div>

<!-- Move this outside the loop -->
<div id="files-list"></div>

<%= link_to "New folder", new_folder_path %>
